
#include<stdio.h>

int main()
{
	int i = 0;
	for (i = 1; i <= 9; i++)//i代表的是行数，99乘法表一共有九行
	{	
		//进入循环，第一行 ，用j来控制有多少列
		int j = 1;
		for (j = 1; j <=i; j++)//第一行一列，第二行二列，以此类推
		{
			printf("%d*%d=%2d ",i,j,i*j);//%2d，打印这个数字打印两位，不够的会在前面用空格补齐，右对齐
		}
		printf("\n");
	}
	return 0;
}
